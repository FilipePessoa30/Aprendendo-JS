const estudante = "Fernando";

const estaAprovado = true;

if (estaAprovado) {
  console.log(`${estudante} está aprovado`);
} else {
  console.log(`${estudante} não está aprovado`);
}

if (estudante === "Fernando") {
  console.log(`Olá, ${estudante}`);
} else {
  console.log("quem é você?");
}
